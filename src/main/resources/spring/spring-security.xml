<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:security="http://www.springframework.org/schema/security"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
                            http://www.springframework.org/schema/security
                            http://www.springframework.org/schema/security/spring-security.xsd">


    <security:http auto-config="true">
        <security:intercept-url pattern="/users*" access="ROLE_ADMIN"/>
        <security:http-basic />

    </security:http>

    <security:jdbc-user-service id="userService" data-source-ref="dataSource" users-by-username-query="SELECT users.name, users.password, 'true' as enabled FROM users WHERE users.name = ?"
                                authorities-by-username-query="SELECT users.name, user_roles.role as authorities FROM users, user_roles WHERE users.name = ? AND users.id = user_roles.user_id"/>

    <security:authentication-manager>
        <security:authentication-provider user-service-ref="userService"/>
            <!--<security:jdbc-user-service data-source-ref="dataSource"-->
                                        <!--users-by-username-query="SELECT users.name, users.password, 'true' as enabled FROM users WHERE users.name = ?"-->
                                        <!--authorities-by-username-query="SELECT users.name, user_roles.role as authorities FROM users, user_roles WHERE users.name = ? AND users.id = user_roles.user_id"/>-->
    </security:authentication-manager>
</beans>